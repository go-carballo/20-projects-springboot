spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/to_do_list
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080